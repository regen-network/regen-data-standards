---
import LI from './Tree.li.astro';
import type {Item} from './Tree.li.astro';
interface Props {
    items: Item[];
}

const {items} = Astro.props;
---
<ul>
    {items.map((item) => (
        <LI {...item} />
    ))}
</ul>